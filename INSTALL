# Faire un environnement local d'install pour les packages
python3 -m venv .venv
source .venv/bin/activate

# pip install flask peewee
pip install -r requirements.txt

# si besoin créer/initialiser la DB
python3 init_db.py

./run.sh
